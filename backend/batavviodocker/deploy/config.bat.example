@echo off
rem Build locale: docker-compose.prod.yml (come cartella prod).
rem Server VPS: docker-compose.yml (unico compose presente su /root/app/backend).

set "DEPLOY_HOST=173.212.220.20"
set "DEPLOY_USER=root"
set "DEPLOY_PATH=/root/app/backend"
set "DEPLOY_COMPOSE_FILE=docker-compose.yml"
set "DEPLOY_GIT_MERGE=1"
set "DEPLOY_GIT_PUSH=1"
set "DEPLOY_USE_CACHE=1"
rem set "DEPLOY_SSH_KEY=%USERPROFILE%\.ssh\id_ed25519"
