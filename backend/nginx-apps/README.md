# Drop-in configs for extra apps (mounted into AM nginx as /etc/nginx/apps-enabled).
# On the server, put files in /root/nginx-apps/ instead (or symlink from each app repo).
# Example: password.colorsdev.tech.conf from Password-Manager repo.
