# Build Docker: non ignorare local.env così docker-compose.localhost (FRONTEND_USE_LOCAL_ENV=1) può copiarlo.
# Per immagini “pulite” in CI, assicurati che local.env non sia nel contesto o non sia committato.
node_modules
.next
.git
*.md
.env*.local